Useful git commands
git init: initialises git
git status: shows status of git
git add --all: add's all changes/directories
git commit -m " ": commits git but need to add first and needs a meaningful commit message. Always commit before pushing
